# UI Helper icon

The active icon is the selection-corners version reconstructed from the user's
Figma adjustments, not the earlier U monogram described below.
Source: `ui-helper-figma-redrawn.svg`; rendered PNG: `ui-helper-figma-redrawn.png`.
Extension sizes (16, 32, 48, 128) are rendered directly from the SVG with
Sharp, preserving alpha transparency.
Do not use Quick Look thumbnails as production assets: they add a white matte.

## Historical U monogram prompt (not the active icon)

Use case: logo-brand. Create one finished square browser extension icon for UI Helper, a DOM selection and visual feedback developer tool. Direction: A · 极简 U 字标. Subject: A single bold white geometric U monogram with a small vivid blue square integrated into its upper-right terminal, hinting at a selected UI element. Extremely simple distinctive silhouette, no other symbols. Style: premium minimal flat vector-like graphic, crisp clean edges, readable at 16px, strong consistent shape weights. Palette matches the existing UI: charcoal #222222, vivid selection blue #087ff5, white. Center the emblem within a charcoal rounded-square tile occupying 90% of canvas; generous balanced inner padding, truly transparent outside tile corners. Exactly one icon, 1024x1024. No captions, no option labels inside the image, no extra letters except the U if specified, no mockup, no texture, no gradients, no glow, no shadows, no 3D. Clearly distinct professionally finished icon design.
